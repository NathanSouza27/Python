#Make a program that reads an integer and shows your successor and predecessor on the screen.

n = int(input('Type it a value:'))

a = n - 1
s = n + 1

print('The predecessor de {} is {} and successor is {}'.format(n, a, s))